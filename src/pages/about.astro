---
import ProfileCard from "../components/ProfileCard.astro";
import Section from "../components/Section.astro";
import Layout from "../layouts/Layout.astro";

import { db, Profile } from 'astro:db';

const profiles = await db.select().from(Profile)

---

<Layout title="About Us">
  <Section>
    <p class="text-3xl font-semibold p-3">
      Giving every student at Imperial the opportunity to
      hear and respond to the gospel of Jesus Christ.
    </p>
    <div class="p-3">
      <p>
        From churches across London (and the world), we
        unite on campus with the vision of spreading the
        good news of the gospel on campus.
      </p>
      <br />
      <p>
        We work with and are supported by UCCF. You can
        check out our <a
          class="underline hover:text-gray-300"
          href="https://www.uccf.org.uk/about/doctrinal-basis"
        >
          doctrinal basis</a
        >.
      </p>
      <br />
      <p>
        We're led by a small committee, but our events are
        organised and run by all of our members.
      </p>
    </div>
  </Section>
  <Section
    lightBackground
    additionalClass="md:items-stretch md:text-sm text-sm flex-wrap"
  >
    {profiles.map((profile) => (
      <ProfileCard
        picture={profile.picture}
        name={profile.name}
        role={profile.role}
        verse={profile.verse}
        bio={profile.bio}
      />
    ))}
  </Section>
</Layout>
